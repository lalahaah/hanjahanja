# 한자한자 — Google Play 출시 프로세스 (순서대로)

마지막 갱신: 2026-08-16. 조직(organization) Play Console 계정 사용 기준 — 12명/14일 비공개 테스트 요건 없음.

## 이미 끝난 것
- ✅ 앱 아이콘 / 적응형 아이콘 / 스플래시 (`scripts/generate-icons.py`)
- ✅ 개인정보처리방침: https://hanja-fadd0.web.app/privacy (한/영)
- ✅ 웹사이트: https://hanja-fadd0.web.app
- ✅ Firebase Functions **프로덕션 배포 완료** (`hanjaLookup`, Secret Manager에 GEMINI_API_KEY 등록됨)
- ✅ `app.json`에 패키지명 확정: `com.nextidealab.hanjahanja`, 앱 이름 "한자한자"로 변경

---

## STEP 1. Expo 계정으로 EAS 로그인 (직접 해야 함)
터미널에서:
```bash
npx eas login
```
- 기존 Expo 계정이 있으면 그 계정으로, 없으면 이메일 입력 후 안내에 따라 새로 만들면 됨
- 로그인되면 이 컴퓨터에 세션이 저장되어서, 이후 빌드 명령은 내가 대신 실행해줄 수 있어

## STEP 2. EAS 빌드 설정 (로그인 후 내가 진행)
```bash
npx eas build:configure
```
- Expo 서버에 프로젝트를 새로 등록하고 `eas.json`을 생성함
- Android부터 진행 (iOS는 나중에 필요할 때 추가 설정 — Apple Developer 계정 별도 필요)

## STEP 3. 프로덕션 빌드 생성 (내가 진행)
```bash
npx eas build --platform android --profile production
```
- Android 앱 서명 키(keystore)를 처음 만들 때 "Generate new keystore?" 물어보면 **Yes** — EAS가 안전하게 대신 관리해줌
- 클라우드에서 빌드되며 보통 10~20분 소요
- 끝나면 `.aab` 파일 다운로드 링크가 나옴 (Play Store 업로드용 포맷)

## STEP 4. Play Console 접속 (직접 해야 함)
1. https://play.google.com/console 접속, `postaiji9@gmail.com` 로그인
2. 화면 왼쪽 위 또는 계정 선택 화면에서 **조직 계정**으로 전환 (12명/14일 테스트 요건 없는 쪽)
3. "앱 만들기" 클릭
   - 앱 이름: `한자한자`
   - 기본 언어: 한국어
   - 앱 또는 게임: **앱**
   - 무료 또는 유료: **무료**
   - 하단 선언 체크박스(개발자 프로그램 정책, 미국 수출 법률) 모두 체크 → 만들기

## STEP 5. App content (왼쪽 메뉴 "정책 > 앱 콘텐츠") — 순서대로

이 섹션의 여러 하위 항목을 위에서부터 순서대로 채우면 됨:

1. **개인정보처리방침**: URL 입력란에 `https://hanja-fadd0.web.app/privacy` 붙여넣기
2. **앱 액세스 권한**: "모든 기능을 별도 절차 없이 사용 가능" 선택 (로그인/결제 없음)
3. **광고**: "아니요, 내 앱에는 광고가 포함되어 있지 않습니다"
4. **콘텐츠 등급**: 설문 시작 → 이메일 입력 → 카테고리는 **참고 자료(Reference)** 또는 **교육** 선택 → 폭력/성적 콘텐츠/도박/공포 등 대부분 질문에 **"아니요"** → 사용자 생성 콘텐츠·사용자 간 소통 기능도 **"아니요"** → 제출하면 등급 인증서 발급됨
5. **타겟층 및 콘텐츠**:
   - 타겟 연령층에 **"만 13세 미만 아동" 포함**해서 선택 (실제 사용 대상이므로)
   - "아동의 흥미를 끌 수 있는 앱인가요?" → **예**
   - 스토어 등록정보가 아동에게 노출되는 방식 관련 질문 → 그대로 안내에 따르면 됨 (아래 조건 이미 충족: 광고 없음, 회원가입 없음, 추적 없음)
6. **데이터 안전 섹션**: 아래 표 그대로 입력

   | 질문 | 답변 |
   |---|---|
   | 앱이 사용자 데이터를 수집 또는 공유하나요? | 예 |
   | 데이터 유형 | "앱 활동" 중 **검색 기록** 1개만 체크. 개인 식별정보(이름/이메일/전화/위치 등)는 전부 미체크 |
   | 이 데이터는 수집되나요? | 예 |
   | 이 데이터는 공유되나요? | 예 — 제3자(Google Gemini API)에 전송되어 결과 생성에 사용됨 |
   | 수집 목적 | 앱 기능 제공 |
   | 전송 중 암호화 여부 | 예 (HTTPS) |
   | 사용자가 삭제 요청 가능한가요? | 예 |
7. **정부 앱 / 금융 기능 / 뉴스 앱 / 코로나19 추적** 등 나머지 선언 → 전부 **해당 없음/아니요**

## STEP 6. 스토어 등록정보 ("성장 > 스토어 등록정보 > 기본 스토어 등록정보")

**앱 이름**: 한자한자

**짧은 설명 (80자 이내)**
```
모르는 단어를 입력하면 한자와 뜻, 유래, 사자성어까지 쉽게 풀이해주는 어린이 한자 학습 앱
```

**전체 설명**
```
한자한자는 아이들이 모르는 한글 단어를 입력하면, 그 단어를 이루는 한자의 뜻과 유래를
쉽고 재미있게 풀이해주는 앱이에요.

📜 한자 이야기 — 한자가 어떻게 만들어졌는지 재미있는 유래를 알려줘요
✏️ 예문 — 실생활에서 어떻게 쓰이는지 자연스러운 예문을 보여줘요
🔗 비슷한 단어 — 같은 한자가 쓰인 다른 단어도 함께 배워요
🧧 사자성어 — 관련된 사자성어까지 확장해서 배울 수 있어요
⭐ 즐겨찾기 — 마음에 드는 단어는 저장해두고 다시 볼 수 있어요

한자능력검정시험(급수) 대비용 암기 앱이 아니라, 아이가 스스로 궁금한 단어를 찾아보며
자연스럽게 한자 실력을 키우는 학습 도구예요.
```

**앱 아이콘**: `assets/images/icon.png` 업로드 (512x512 요구 — 필요하면 리사이즈해서 줄게)

**스크린샷** (휴대전화용 최소 2장, 최대 8장):
1. `npx expo start` 로 앱 실행 (실기기 또는 에뮬레이터)
2. 단어 몇 개 검색해서 결과 화면, 즐겨찾기 화면 등 캡처
3. 캡처한 이미지를 그대로 업로드 (별도 편집 불필요)

**기능 그래픽 (1024x500)**: 필요하면 요청해줘, 만들어줄게

**카테고리**: 교육 (Education)

**연락처 이메일**: nextidealab.ai@gmail.com

**웹사이트**: https://hanja-fadd0.web.app

## STEP 7. 릴리스 만들기 ("품질 > 테스트 > 내부 테스트" 먼저 권장)

1. 왼쪽 메뉴 "테스트 > 내부 테스트" → "새 버전 만들기"
2. STEP 3에서 받은 `.aab` 파일 업로드
3. 출시 노트 작성 (예: "첫 출시 버전입니다")
4. 저장 → 검토 → 출시
5. 테스터 이메일 추가 (본인, 가족 등 몇 명) → 링크 공유해서 실기기로 설치·확인
6. 문제없으면 같은 빌드를 "프로덕션" 트랙으로 승격 (왼쪽 메뉴 "프로덕션" → "버전 만들기" → 같은 aab 선택, 또는 내부테스트 화면에서 "프로덕션으로 승격" 버튼)

## STEP 8. 제출 및 심사 대기
- 프로덕션 버전 "검토를 위해 게시" 클릭
- Google 심사는 보통 며칠 이내 (아동 대상 앱은 조금 더 걸릴 수 있음)
- 반려되면 사유가 이메일/Console에 표시됨 — 그때 같이 대응하면 됨

---

## 지금 당장 막힌 부분
- STEP 1 (`npx eas login`)은 네가 직접 해야 진행 가능해. 로그인 끝나면 알려줘, STEP 2~3은 내가 이어서 진행할게.
